{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "engines": {
    "node": ">=16.14.2"
  },
  "scripts": {
    "prepare-env": "node --loader ts-node/esm ./prepare-env.ts",
    "build": "npm run prepare-env && tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@sctg/nocors-azure": "^0.9.13",
    "agentkeepalive": "^4.2.1",
    "got": "^12",
    "iconv-lite": "^0.6.3"
  },
  "devDependencies": {
    "@azure/functions": "^3.2.0",
    "@types/node": "16.x",
    "typescript": "^4.8.4"
  }
}
